import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 100,
    width: '25%',
    alignItems: 'center',
  },
  thumbnail: {
    height: '96%',
    width: '96%',
  },
});

export default styles;
