import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 120,
    width: '33%',
    alignItems: 'center',
  },
  thumbnail: {
    height: '96%',
    width: '96%',
  },
});

export default styles;
